@model CheckersProject.Models.Board

@{
    ViewData["Title"] = "Home Page";
}
<div class="container">
    <div class="chessboard">
        @foreach (var cell in Model.cells)
        {
            if (cell.colour == teamColour.black)
            {
                if (cell.piece != null)
                {
                    System.Diagnostics.Debug.WriteLine(cell.possibleMoves);
                    <div class="black" id="cell-@cell.index" onclick="showPossibleMoveDiv(this)">
                        @*<div class="checker possible-move" hidden="hidden" id="cell-@cell.index" onclick="makeMove(this)"></div>*@
                        @if (cell.piece.team == teamColour.white)
                        {
                            <div class="checker white-checker" draggable="true"></div>
                        }
                        @if (cell.piece.team  == teamColour.black)
                        {
                            <div class="checker black-checker" draggable="true"></div>
                        }
                    </div>
                }
                else{
                    <div class="black">
                        <div class="checker possible-move" hidden="hidden" id="cell-@cell.index" onclick="makeMove(this)"></div>
                    </div>
                }

            }
            else
            {
                <div class="white">
                </div>
            }
        }
    </div>
</div>